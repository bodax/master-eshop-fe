plugins {
    id "com.moowork.node" version "1.3.1"
    id 'base'
}

repositories {
    mavenCentral()
}

tasks.register('appNpmBuild', NpmTask) {
    args = ['run', 'build']
}

assemble.dependsOn(appNpmBuild)